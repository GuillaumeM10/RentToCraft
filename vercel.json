{
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm run build:libs && pnpm --filter ui run build",
  "outputDirectory": "apps/ui/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "functions": {
    "apps/ui/pages/api/**/*.{js,ts}": {
      "runtime": "edge"
    }
  },
  "env": {
    "NODE_ENV": "production",
    "PNPM_VERSION": "latest"
  }
}
